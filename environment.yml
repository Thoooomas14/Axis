name: axis_env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - numpy
  - pip
  - pip:
      - torch~=2.2.1 --index-url https://download.pytorch.org/whl/cu121
      - mlagents
